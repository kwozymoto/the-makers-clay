# Normalise to LF in the repo and the working tree — Cloudflare Pages builds
# on Linux, and this keeps Windows checkouts from introducing CRLF churn.
* text=auto eol=lf

*.woff2 binary
*.jpg   binary
*.jpeg  binary
*.png   binary
*.webp  binary
